# 前沿追踪 · 2026 年 5 月：值得关注的 Agent 进展

> 这是「前沿追踪」层的示例条目。每个月（或每次有重要动态时）新建一篇，按日期倒序自动排在最前——无需手动维护编号，角标会自动显示日期。

## 本月看点

- **模型发布**：记录本月值得关注的新模型与能力边界变化。
- **关键论文**：挑选 1–3 篇对 Agent 工程有实际影响的论文，给出一句话要点。
- **协议与生态**：MCP、工具生态、运行时框架的重要更新。
- **产品动态**：一线 Agent 产品的形态演进与值得借鉴的设计。

## 对全栈 Agent 工程的影响

简要说明这些动态分别落在「应用 / 编排 / 运行时 / 模型」哪一层，方便和体系化课程交叉阅读。

:::方法 消融实验
逐个拿掉系统里的一个组件（reranker、记忆、某个工具）各跑一遍，看指标怎么变，用来定位到底是哪一部分在真正起作用。

> 内联标注块写法：`:::方法 标题` 起、单独一行 `:::` 收，正文支持 markdown。类型可用 **方法 / 观点 / 启发 / 提醒**。
:::

:::观点 我的看法
评测先行，再谈优化——哪怕只有 20 条手工标注轨迹，也比凭感觉调 prompt 靠谱。
:::

下面这条是**引用**自札记的独立观点（只在 `notes.json` 维护一份，用 `![[id]]` 嵌进来）：

![[rag-chunking]]

---

*如何新增前沿条目：在 `posts/` 下新建 `frontier-YYYY-MM.md`，并在 `posts/manifest.json` 的 `posts` 数组里加一条 `"layer": "frontier"` 的记录（带 `date` 字段即可），角标编号会自动生成。*
